pattern = "*_test"
if ctx.options.test != None:
    pattern = ctx.options.test + "_test"

test_binaries = ctx.path.get_bld().ant_glob(pattern)
for test_binary in test_binaries:
   ctx.exec_command(test_binary.abspath())
